node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# local env
.env
.env.local

# database
*.db
*.db-journal
prisma/dev.db*

# roster build cache (regenerable; the roster output itself IS committed)
.cache/

# sprites downloaded by `roster:build --sprites`
public/sprites/

.DS_Store

# draft report PDFs: regenerated on every reprice, so a binary diff each time is noise. The
# committed record is data/draft-guide.html, next to its source data/draft-guide.json.
reports/

# the private scouting edition of the draft report, and its notes — one manager's plan, which
# must never reach history the rest of the league can read
private/
